include <../lib/collar-tag-lib.scad>;
$fn = 72;

// Single-color STL — body + raised text/edge fused into one solid for
// single-filament printers. The text and border are still physically
// raised on the top face; they just don't change color.

single();
