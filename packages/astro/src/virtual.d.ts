declare module 'virtual:3d-gallery' {
  import type { RuntimeManifest } from '@3d-gallery/model-core';
  export const manifest: RuntimeManifest;
  export const artifactBase: string;
  export const keySchema: string;
  export default manifest;
}
