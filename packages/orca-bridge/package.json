{
  "name": "@3d-gallery/orca-bridge",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "type": "module",
  "description": "Read-only reader for an OrcaSlicer install: presets with resolved inheritance, app config history, and sliced 3MF projects. Reconciles a finished Orca job against known records so it can be replayed in the gallery. Never writes to the Orca config.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./mcp": "./src/mcp.ts",
    "./devstore": "./src/devstore.ts"
  },
  "files": [
    "src",
    "README.md"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "fflate": "^0.8.2"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^5.7.0",
    "vitest": "4.0.18"
  }
}
